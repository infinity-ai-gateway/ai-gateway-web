# UI 改动协作记录（2026-08-01）

## 背景

API 侧完成 `instance_pool` 数据模型重设计（`ai-gateway-api/design-docs/modifications/2026-07-31-api-define-modification-and-bugfix/change-summary.md`），Web 侧 `design-docs/api-define/OpenAPI接口定义/clusters.md` 已同步为 `name`/`addr`/`port`/`weight`。

当前 UI 源码仍按上一轮对齐的 `hostname`/`ip`/`ports` 提交，需重新整理变更文档。

## 用户要求

- 文档目录使用 **当前日期** + 后缀 **`ui-optimize`**：`2026-08-01-ui-optimize`
- **本次修改需参考产品原型设计**；实例列表端口列**去掉 `Default：` 前缀**（与 API `port` 整数一致）
- 整理 Skill 文件，规范后续「对照 api-define + prototype-design → 输出 modifications」流程

## 本次范围

| 维度 | 范围 |
|------|------|
| 接口 | `/clusters` — `instance_pool`、`basic.protocol`、`passive_health_check.host` |
| 原型 | `assets/js/cluster-upsert.js` 实例配置步骤、复查页 |
| 源码 | `src/modules/Clusters/components/InstancePool.vue` 等 |
| 不在范围 | APIKey、Entity、User（无本次 API 变更）；`AIInstancePool`（独立 `/alb-pool` 模块） |

## 产出文件

- [openapi-doc-diff.md](./openapi-doc-diff.md)
- [prototype-ui-compare.md](./prototype-ui-compare.md)
- [ui-code-changes.md](./ui-code-changes.md)

## Skill

项目 Skill：`.cursor/skills/ui-optimize-doc/SKILL.md`
